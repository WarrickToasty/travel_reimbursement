<%= form_for(employee) do |f| %>
  <% if employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="sap_number">Sap Number</label>
    <%= f.text_field :sap_number, class: "form-control", id: "sap_number" %>
  </div>

  <div class="form-group">
    <label for="email">Email</label>
    <%= f.text_field :email, class: "form-control", id: "email" %>
  </div>

  <div class="form-group">
    <label for="department">Department</label>
    <%= f.text_field :department, class: "form-control", id: "department" %>
  </div>

  <div class="form-group">
    <label for="first_name">First Name</label>
    <%= f.text_field :first_name, class: "form-control", id: "first_name" %>
  </div>

  <div class="form-group">
    <label for="last_name">Last Name</label>
    <%= f.text_field :last_name, class: "form-control", id: "last_name" %>
  </div>

  <div class="form-group">
    <label for="phone_number">Phone Number</label>
    <%= f.text_field :phone_number, class: "form-control", id: "phone_number" %>
  </div>

  <div class=sr-only form-group">
    <%= f.text_field :username %>
  </div>

  <div class="sr-only form-group">
    <label for="sap_number">Sap Number</label>
    <%= f.text_field :user_id %>
  </div>

  <%= f.submit class: "btn-login btn btn-primary" %>
<% end %>
